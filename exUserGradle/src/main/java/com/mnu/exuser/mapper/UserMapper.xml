<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mnu.exuser.mapper.UserMapper">
	<!-- ID 중복검사 -->
	<select id="userIdCheck" resultType="int">
		select count(*) from tbl_user where userid=#{userid}
	</select>
	
	<!-- 회원등록 -->
	<insert id="userInsert">
		insert into tbl_user(userid, name, passwd, tel, email) 
					values(#{userid},#{name},#{passwd},#{tel},#{email})
	</insert>


</mapper>