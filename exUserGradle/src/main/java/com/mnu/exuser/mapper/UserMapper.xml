<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mnu.exuser.mapper.UserMapper">
	<!-- ID 중복검사 -->
	<select id="userIdCheck" resultType="int">
		select count(*) from tbl_user where userid=#{userid}
	</select>
	
	<!-- 회원등록 -->
	<insert id="userInsert">
		insert into tbl_user(userid, name, passwd, tel, email) 
					values(#{userid},#{name},#{passwd},#{tel},#{email})
	</insert>

	<!-- 로그인(ID가 존재하면 비번 반환) -->
	<select id="userLoginPass" resultType="string">
		select passwd from tbl_user where userid=#{userid}
	</select>
	
	<select id="userLoginSearch" resultType="string">
		select passwd from tbl_user where userid=#{userid}
	</select>

	<!-- 로그인 성공시 session 저장용  -->
	<select id="userLogin" resultType="com.mnu.exuser.domain.UserDTO">
		select * from tbl_user where userid=#{userid}
	</select>
	
	<!-- 로그인시 마지막로그인한 날자 update -->
	<update id="userLoginLastTimeUpdate">
		update tbl_user set last_time = now() where userid=#{userid}
	</update>
	
	<!-- 회원정보수정 -->
	<update id="userModify">
		update tbl_user set tel=#{tel}, passwd=#{passwd}, email=#{email}
				where userid=#{userid}
	</update>
	
	<!-- 회원정보 삭제 -->
	<delete id="userDelete">
		delete from tbl_user where userid=#{userid}
	</delete>
	
	<!--  전체 회원 목록 -->
	<select id="userList" resultType="com.mnu.exuser.domain.UserDTO">
		select * from tbl_user order by first_time desc;
	</select>
</mapper>